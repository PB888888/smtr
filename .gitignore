# Secrets — never commit. See .env.example for the template.
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build output
dist/

# Audit and runtime logs (append-only, machine-local)
logs/
*.jsonl

# Review queues generated by the runner
runner/queues/

# OS and editor noise
.DS_Store
*.swp
